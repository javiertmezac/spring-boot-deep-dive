<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Inherit Spring Boot dependency management ONLY (versions). A starter is
         a plain library jar, NOT a bootable app — so no spring-boot-maven-plugin
         and no repackaging here. -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>com.itj.bootcamp</groupId>
    <artifactId>taskflow-spring-boot-starter</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>taskflow-spring-boot-starter</name>
    <description>A custom Spring Boot starter that auto-configures a greeting service</description>

    <properties>
        <java.version>21</java.version>
    </properties>

    <dependencies>
        <!-- @AutoConfiguration, @ConditionalOn*, @ConfigurationProperties support -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>

        <!-- Generates metadata for our @ConfigurationProperties (IDE hints) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>
</project>
